name: boilerplate-test

services:
  redis-test-db:
    image: redis:7.0
    restart: no
    networks:
      - test-network
    ports:
      - "6379:6379"

networks:
  test-network:
    name: test-network
